# Админ рөлін қосу - Мысал

## 📧 Email-ді қалай табуға болады?

### 1. Сайтта тіркелген email-ді пайдаланыңыз

Егер сіз сайтта тіркелген болсаңыз, тіркелген email-ді пайдаланыңыз.

### 2. Мысал email-дер:

```
admin@example.com
user@example.com
test@test.com
myemail@gmail.com
```

## 🚀 Скриптті іске қосу

### Әдіс 1: npm script арқылы

```powershell
npm run make-admin your-email@example.com
```

**Мысал:**
```powershell
npm run make-admin admin@example.com
```

### Әдіс 2: npx арқылы

```powershell
npx tsx scripts/make-admin.ts your-email@example.com
```

**Мысал:**
```powershell
npx tsx scripts/make-admin.ts admin@example.com
```

## 📝 Толық мысал

### 1. Алдымен тіркелу (егер әлі тіркелмеген болсаңыз)

1. Сайтқа өтіңіз: `http://localhost:3000`
2. "Кіру" батырмасын басыңыз
3. "Тіркелу" табын таңдаңыз
4. Email: `admin@example.com` (немесе өз email-іңіз)
5. Пароль: `password123` (немесе өз пароліңіз)
6. "Тіркелу" батырмасын басыңыз

### 2. Админ рөлін қосу

Терминалда:

```powershell
npm run make-admin admin@example.com
```

Немесе:

```powershell
npx tsx scripts/make-admin.ts admin@example.com
```

### 3. Нәтиже

```
📧 Пайдаланушыны іздеу: admin@example.com
🔄 Рөлді өзгерту: user → admin

==================================================
✅ Пайдаланушы админ рөліне ие болды!
==================================================
📧 Email: admin@example.com
👤 Аты: admin
🔑 Рөл: admin

💡 Енді сайтқа кіріп, /admin бетіне өтуге болады!
```

### 4. Админ панеліне өту

1. Сайтқа кіріңіз: `http://localhost:3000`
2. `/admin` бетіне өтіңіз: `http://localhost:3000/admin`
3. Админ панелі ашылады!

## ⚠️ Ескертулер

1. **Email дұрыс болуы керек** - Сайтта тіркелген email-ді пайдаланыңыз
2. **Алдымен тіркелу керек** - Егер пайдаланушы жоқ болса, алдымен тіркелу керек
3. **Базаға байланыс** - PostgreSQL сервері іске қосылған болуы керек

## 🔍 Пайдаланушыларды көру

Егер қандай email-дер тіркелгенін білгіңіз келсе:

```powershell
npm run db:studio
```

Браузерде `http://localhost:5555` адресін ашып, `User` модельін қараңыз.

## ❓ Көмек

Егер қателер туындаса:

1. **Пайдаланушы табылмады:**
   - Алдымен сайтта тіркелу керек
   - Email дұрыс екенін тексеріңіз

2. **Базаға байланыс қатесі:**
   - `.env.local` файлында `DATABASE_URL` дұрыс екенін тексеріңіз
   - PostgreSQL сервері іске қосылғанын тексеріңіз












