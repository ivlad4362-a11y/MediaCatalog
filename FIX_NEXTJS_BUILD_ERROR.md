# Next.js Build Қатесін Түзету

## ❌ Қате

```
Error: ENOENT: no such file or directory, open 
'C:\Android\MediaCatalog\.next\server\pages\_document.js'
```

## 🔍 Мәселе

`.next` папкасындағы build файлдары жоқ немесе бұзылған. Бұл Next.js серверінің беттерді генерациялауға мүмкіндігі жоқ дегенді білдіреді.

## ✅ Шешім

### 1. Серверді тоқтату
```powershell
Get-Process -Name node -ErrorAction SilentlyContinue | Stop-Process -Force
```

### 2. .next папкасын жою
```powershell
Remove-Item -Path ".next" -Recurse -Force
```

### 3. Серверді қайта іске қосу
```powershell
npm run dev
```

## 📝 Нәтиже

Сервер қайта іске қосылғанда `.next` папкасы автоматты түрде қайта құрылады және барлық қажетті файлдар генерацияланады.

## ⚠️ Ескерту

Бірінші рет build біраз уақыт алуы мүмкін (1-2 минут). Терминалда "Ready" хабарламасын күтіңіз.

## 🔄 Егер мәселелер жалғаса

1. **node_modules және .next жою:**
   ```powershell
   Remove-Item -Path "node_modules" -Recurse -Force
   Remove-Item -Path ".next" -Recurse -Force
   ```

2. **Пакеттерді қайта орнату:**
   ```powershell
   npm install --legacy-peer-deps
   ```

3. **Серверді қайта іске қосу:**
   ```powershell
   npm run dev
   ```












