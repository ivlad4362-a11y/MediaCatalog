# .env.local файлын орнату нұсқаулары

## ✅ Файл құрылды!

`.env.local` файлы құрылды. Енді оны өзгерту керек.

## 📝 Қадамдық нұсқаулар:

### 1. Файлды ашу

VS Code-та немесе басқа мәтін редакторында `.env.local` файлын ашыңыз.

### 2. PostgreSQL паролін орнату

`DATABASE_URL` жолында `your_password` орнына өз PostgreSQL пароліңізді қойыңыз:

**Бұрын:**
```env
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/mediacatalog?schema=public"
```

**Кейін (мысал):**
```env
DATABASE_URL="postgresql://postgres:МеніңПаролім123@localhost:5432/mediacatalog?schema=public"
```

### 3. JWT_SECRET-ті өзгерту (міндетті емес, бірақ ұсынылады)

`JWT_SECRET`-ті кемінде 32 таңбадан тұратын кездейсоқ мәтінге өзгертіңіз:

**Мысал:**
```env
JWT_SECRET="my-super-secret-key-for-jwt-tokens-2024-mediacatalog-app-secure"
```

### 4. Файлды сақтау

Файлды сақтаңыз (Ctrl+S).

## 🔍 PostgreSQL паролін қалай табуға болады?

Егер парольді білмесеңіз:

1. **PostgreSQL орнату кезінде қойылған парольді есте сақтаңыз**
2. **Немесе PostgreSQL паролін қайта орнатыңыз:**
   ```bash
   # Windows Services-та PostgreSQL сервисін тоқтатыңыз
   # Содан кейін:
   psql -U postgres
   # Егер кіре алмасаңыз, парольді қайта орнатыңыз
   ```

## ⚠️ Маңызды ескертулер:

1. **Парольді дұрыс қойыңыз** - базаға байланыс үшін қажет
2. **JWT_SECRET** - кемінде 32 таңба болуы керек
3. **Файлды сақтаңыз** - өзгерістерді сақтау үшін
4. **Git-ке қоспаңыз** - `.env.local` файлы `.gitignore`-да, бұл дұрыс

## ✅ Тестілеу:

Файлды сақтағаннан кейін:

```bash
# Серверді іске қосыңыз
npm run dev
```

Егер қателер болмаса, бәрі дұрыс!

## 🆘 Мәселелер:

### Егер "Can't reach database" қатесі болса:

1. PostgreSQL сервері іске қосылғанын тексеріңіз
2. Парольді дұрыс қойғаныңызды тексеріңіз
3. База құрылғанын тексеріңіз: `psql -U postgres -l`

### Егер база жоқ болса:

```bash
# PostgreSQL-ге кіріңіз
psql -U postgres

# Базаны құрыңыз
CREATE DATABASE mediacatalog;

# Шығыңыз
\q
```

Содан кейін:
```bash
npm run db:generate
npm run db:push
```












