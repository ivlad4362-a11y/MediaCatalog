# Базадағы мәліметтерді көру

## 📊 Мәліметтерді көру әдістері

### 1️⃣ Скрипт арқылы (Терминал)

```powershell
npm run view-db
```

Немесе:

```powershell
npx tsx scripts/view-database.ts
```

**Нәтиже:**
- Пайдаланушылар
- Фильмдер
- Кітаптар
- Ойындар
- Жанрлар
- Статистика

---

### 2️⃣ Prisma Studio арқылы (Графикалық интерфейс)

```powershell
npm run db:studio
```

Браузерде `http://localhost:5555` адресін ашыңыз.

**Мүмкіндіктер:**
- Барлық таблицаларды көру
- Мәліметтерді өзгерту
- Жаңа мәліметтер қосу
- Мәліметтерді жою

---

### 3️⃣ API арқылы

#### Барлық фильмдер:
```bash
GET http://localhost:3000/api/movies
```

#### Барлық кітаптар:
```bash
GET http://localhost:3000/api/books
```

#### Барлық ойындар:
```bash
GET http://localhost:3000/api/games
```

#### Пайдаланушылар (тек админ):
```bash
GET http://localhost:3000/api/users
Authorization: Bearer <token>
```

---

### 4️⃣ Базаға тікелей қосылу (PostgreSQL)

```powershell
psql -U postgres -d MediaCatalog
```

Содан кейін SQL командаларын пайдаланыңыз:

```sql
-- Барлық пайдаланушыларды көру
SELECT id, email, name, role FROM "User";

-- Барлық материалдарды көру
SELECT id, title, type, year, rating FROM "MediaItem";

-- Фильмдерді көру
SELECT id, title, year, rating FROM "MediaItem" WHERE type = 'movie';

-- Кітаптарды көру
SELECT id, title, year, rating FROM "MediaItem" WHERE type = 'book';

-- Ойындарды көру
SELECT id, title, year, rating FROM "MediaItem" WHERE type = 'game';
```

---

## 📋 Мысал нәтиже

```
📊 Базадағы мәліметтер:

============================================================

👥 Пайдаланушылар (2):
------------------------------------------------------------
  1. admin@example.com
     Аты: Админ
     Рөл: admin
     Тіркелген: 11/28/2025

  2. user@example.com
     Аты: Пайдаланушы
     Рөл: user
     Тіркелген: 11/28/2025

🎬 Фильмдер (10):
------------------------------------------------------------
  1. Inception (2010)
     Рейтинг: 8.8 | Популярлық: 95
  2. The Dark Knight (2008)
     Рейтинг: 9.0 | Популярлық: 98
  ...

📚 Кітаптар (5):
------------------------------------------------------------
  1. Dune (1965)
     Рейтинг: 8.5 | Популярлық: 90
  ...

🎮 Ойындар (5):
------------------------------------------------------------
  1. The Witcher 3: Wild Hunt (2015)
     Рейтинг: 9.5 | Популярлық: 95
  ...

🏷️ Жанрлар (15):
------------------------------------------------------------
  Action, Adventure, Comedy, Drama, Fantasy, ...

📈 Статистика:
------------------------------------------------------------
  Барлығы материалдар: 20
  Фильмдер: 10
  Кітаптар: 5
  Ойындар: 5
  Пайдаланушылар: 2
  Жанрлар: 15

============================================================
✅ Мәліметтер көрсетілді!
```

---

## 🔍 Детальды мәліметтер

### Prisma Studio арқылы:

1. `npm run db:studio` командасын іске қосыңыз
2. Браузерде `http://localhost:5555` адресін ашыңыз
3. Таблицаларды таңдап, мәліметтерді көруге болады

### API арқылы (JSON формат):

```bash
# Фильмдер
curl http://localhost:3000/api/movies

# Кітаптар
curl http://localhost:3000/api/books

# Ойындар
curl http://localhost:3000/api/games
```

---

## 💾 Мәліметтерді экспорттау

### JSON форматта:

```powershell
# Фильмдерді файлға сақтау
curl http://localhost:3000/api/movies -o movies.json

# Кітаптарды файлға сақтау
curl http://localhost:3000/api/books -o books.json

# Ойындарды файлға сақтау
curl http://localhost:3000/api/games -o games.json
```

---

## ⚠️ Ескертулер

1. **Prisma Studio** - Графикалық интерфейс, мәліметтерді өзгертуге болады
2. **API** - JSON форматта, бағдарламалық түрде пайдалануға ыңғайлы
3. **Скрипт** - Терминалда тез көру үшін
4. **PostgreSQL** - Тікелей базаға қосылу, SQL командаларын пайдалану

---

## ✅ Қысқаша командалар

```powershell
# Мәліметтерді көру (терминал)
npm run view-db

# Графикалық интерфейс
npm run db:studio

# API арқылы (браузерде)
http://localhost:3000/api/movies
http://localhost:3000/api/books
http://localhost:3000/api/games
```












