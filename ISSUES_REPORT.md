# Сайттағы мәселелердің толық есебі

## 🔴 Негізгі мәселелер

### 1. **.env файлы жоқ** ⚠️ КРИТИКАЛЫҚ
**Мәселе:** `.env` немесе `.env.local` файлы жоқ, сондықтан:
- `DATABASE_URL` - база деректеріне байланыс жоқ
- `JWT_SECRET` - аутентификация жұмыс істемейді
- `NEXT_PUBLIC_BASE_URL` - API сұраулары дұрыс жұмыс істемейді

**Қажетті файл:** `.env.local`
```env
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/mediacatalog?schema=public"
JWT_SECRET="your-secret-key-change-in-production-min-32-chars"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
```

### 2. **API маршруттары auth-mock пайдаланады** ⚠️ МАҢЫЗДЫ
**Мәселе:** API маршруттары база деректеріне емес, mock файлдарға байланысқан:
- `app/api/auth/register/route.ts` → `@/lib/auth-mock` пайдаланады
- `app/api/auth/login/route.ts` → `@/lib/auth-mock` пайдаланады
- `app/api/auth/me/route.ts` → `@/lib/auth-mock` пайдаланады

**Шешім:** `@/lib/auth-mock` орнына `@/lib/auth` пайдалану керек

### 3. **База деректеріне байланыс жоқ** ⚠️ МАҢЫЗДЫ
**Мәселе:** 
- Prisma схемасы бар (`prisma/schema.prisma`)
- Бірақ базаға байланыс жоқ (DATABASE_URL жоқ)
- База кестелері құрылмаған болуы мүмкін

**Шешім:**
1. PostgreSQL базасын құру
2. `.env.local` файлында `DATABASE_URL` орнату
3. `npm run db:push` орындау (Prisma схемасын базаға қосу)

### 4. **База деректері конфигурациясы** ⚠️ МАҢЫЗДЫ
**Мәселе:** 
- `lib/db.ts` базаға байланысқанда қателерді басқарады
- Бірақ базаға байланыс жоқ болса, mock мәліметтерге fallback жасайды
- Бұл сайттың жұмыс істеуін көрсетеді, бірақ деректер сақталмайды

## 📋 Тексеру тізімі

### База деректері:
- [ ] PostgreSQL орнатылған ма?
- [ ] База құрылған ма? (`mediacatalog`)
- [ ] `.env.local` файлында `DATABASE_URL` бар ма?
- [ ] Prisma схемасы базаға қосылған ма? (`npm run db:push`)

### API маршруттары:
- [ ] `/api/movies` - жұмыс істейді ма?
- [ ] `/api/books` - жұмыс істейді ма?
- [ ] `/api/games` - жұмыс істейді ма?
- [ ] `/api/auth/register` - жұмыс істейді ма?
- [ ] `/api/auth/login` - жұмыс істейді ма?
- [ ] `/api/auth/me` - жұмыс істейді ма?

### Фронтенд:
- [ ] Басты бет жүктеледі ма?
- [ ] Фильмдер/Кітаптар/Ойындар беттері жүктеледі ма?
- [ ] Тіркелу/Кіру жұмыс істейді ма?
- [ ] Аутентификация дұрыс жұмыс істейді ма?

## 🔧 Шешімдер

### 1. .env.local файлын құру
```bash
# Түбірде .env.local файлын құрыңыз
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/mediacatalog?schema=public"
JWT_SECRET="your-secret-key-change-in-production-min-32-chars-long"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
```

### 2. API маршруттарын түзету
`app/api/auth/register/route.ts`, `app/api/auth/login/route.ts`, `app/api/auth/me/route.ts` файлдарында:
- `@/lib/auth-mock` → `@/lib/auth` өзгерту

### 3. База деректерін дайындау
```bash
# 1. PostgreSQL базасын құру
psql -U postgres
CREATE DATABASE mediacatalog;
\q

# 2. Prisma клиентін генерациялау
npm run db:generate

# 3. Базаға схеманы қосу
npm run db:push

# 4. Мок мәліметтерді импорттау (қаласаңыз)
npm run import-data
```

### 4. Серверді іске қосу
```bash
npm run dev
```

## 🧪 Тестілеу

### API тестілеу (PowerShell):
```powershell
# Фильмдерді тексеру
Invoke-WebRequest -Uri "http://localhost:3000/api/movies" -Method GET

# Кітаптарды тексеру
Invoke-WebRequest -Uri "http://localhost:3000/api/books" -Method GET

# Тіркелу тексеру
$body = @{
    email = "test@example.com"
    password = "test123"
    name = "Тест"
} | ConvertTo-Json
Invoke-WebRequest -Uri "http://localhost:3000/api/auth/register" -Method POST -Body $body -ContentType "application/json"
```

## ⚠️ Ескертулер

1. **DATABASE_URL** - PostgreSQL пароліңізді дұрыс қойыңыз
2. **JWT_SECRET** - Кемінде 32 таңба болуы керек, production-да өзгертіңіз
3. **База деректері** - Егер базаға байланыс жоқ болса, сайт mock мәліметтермен жұмыс істейді, бірақ деректер сақталмайды



