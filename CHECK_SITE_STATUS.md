# Сайт статусын тексеру

## ✅ Орындалған қадамдар:

1. ✅ Сервер тоқтатылды
2. ✅ `.next` папкасы жойылды
3. ✅ Сервер қайта іске қосылды
4. ✅ `.next` папкасы қайта құрылды

## 🔍 Тексеру:

### 1. Сервер іске қосылғанын тексеру:

Терминалда мынаны көру керек:
```
▲ Next.js 15.2.4
- Local:        http://localhost:3000
```

### 2. Браузерде тексеру:

1. Браузерді ашыңыз
2. `http://localhost:3000` адресін ашыңыз
3. Егер қателер болса, F12 басып Console табын қараңыз

### 3. Егер сайт ашылмаса:

**Терминалда қателерді қараңыз:**
- Қандай қателер көрсетілген?
- Қай файлда қате бар?

**Браузер консольінде:**
- F12 → Console табы
- Қандай қателер көрсетілген?

## ⚠️ Мүмкін мәселелер:

1. **Build уақыты** - Бірінші рет build біраз уақыт алуы мүмкін (1-2 минут)
2. **Порт бос емес** - Егер порт бос емес болса, басқа порт пайдаланылады
3. **Қателер** - Терминалда қателерді қараңыз

## 🔧 Шешімдер:

### Егер сервер іске қосылмаса:

```powershell
# Портты тексеру
netstat -ano | findstr :3000

# Егер порт бос емес болса, процесті тоқтату
Get-Process -Id <PID> | Stop-Process -Force

# Серверді қайта іске қосу
npm run dev
```

### Егер build қателері болса:

```powershell
# node_modules және .next жою
Remove-Item -Path "node_modules" -Recurse -Force
Remove-Item -Path ".next" -Recurse -Force

# Пакеттерді қайта орнату
npm install --legacy-peer-deps

# Серверді іске қосу
npm run dev
```

## ✅ Келесі қадамдар:

1. Терминалда сервердің іске қосылғанын тексеріңіз
2. Браузерде `http://localhost:3000` адресін ашыңыз
3. Егер қателер болса, оларды айтыңыз












