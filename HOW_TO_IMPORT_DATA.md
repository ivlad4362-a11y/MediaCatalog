# Базаға мәлімет енгізу нұсқаулығы

## ✅ Нәтиже

17 элемент сәтті базаға сақталды:
- ✅ 10 фильм
- ✅ 5 кітап  
- ✅ 2 ойын

Кейбір элементтер қазірдің өзінде бар екен (дубликаттар) - бұл қалыпты.

---

## 📋 Мәліметтерді импорттау әдістері

### 1️⃣ Скрипт арқылы (Терминал)

```powershell
npm run import-data
```

Бұл команда барлық mock мәліметтерді базаға импорттайды.

**Артықшылықтары:**
- Жылдам
- Барлық мәліметтерді бірден импорттайды
- Дубликаттарды автоматты түрде өткізіп жібереді

---

### 2️⃣ Админ панелі арқылы (Веб интерфейс)

1. Сайтқа кіріңіз
2. Админ рөліне ие болыңыз (егер жоқ болса, `/api/users/make-admin` арқылы қосыңыз)
3. `/admin` бетіне өтіңіз
4. "Импорттау" батырмасын басыңыз

**Артықшылықтары:**
- Веб интерфейс арқылы оңай
- Нәтижелерді көруге болады
- Қателерді көруге болады

---

## ➕ Жаңа мәліметтер қосу

### Админ панелі арқылы

1. `/admin` бетіне өтіңіз
2. "Жаңа материал қосу" батырмасын басыңыз
3. Форманы толтырыңыз:
   - **Атауы** (title)
   - **Сипаттама** (description)
   - **Жыл** (year)
   - **Рейтинг** (rating)
   - **Жанр** (genre) - бірнеше таңдауға болады
   - **Сурет URL** (coverImage)
   - **Тип** (type) - movie, book, немесе game
4. "Сақтау" батырмасын басыңыз

### API арқылы (Программисттер үшін)

#### Фильм қосу:
```bash
POST /api/movies
Content-Type: application/json
Authorization: Bearer <token>

{
  "title": "Фильм атауы",
  "description": "Сипаттама",
  "year": 2024,
  "rating": 8.5,
  "genre": ["Action", "Drama"],
  "coverImage": "https://example.com/image.jpg",
  "popularity": 85
}
```

#### Кітап қосу:
```bash
POST /api/books
Content-Type: application/json
Authorization: Bearer <token>

{
  "title": "Кітап атауы",
  "description": "Сипаттама",
  "year": 2024,
  "rating": 9.0,
  "genre": ["Fiction", "Sci-Fi"],
  "coverImage": "https://example.com/image.jpg",
  "popularity": 90
}
```

#### Ойын қосу:
```bash
POST /api/games
Content-Type: application/json
Authorization: Bearer <token>

{
  "title": "Ойын атауы",
  "description": "Сипаттама",
  "year": 2024,
  "rating": 9.5,
  "genre": ["Action", "Adventure"],
  "coverImage": "https://example.com/image.jpg",
  "popularity": 95
}
```

---

## 🔍 Мәліметтерді тексеру

### Базада қанша мәлімет бар?

```powershell
# Prisma Studio арқылы (графикалық интерфейс)
npm run db:studio
```

Браузерде `http://localhost:5555` адресін ашыңыз.

### API арқылы:

```bash
# Барлық фильмдер
GET /api/movies

# Барлық кітаптар
GET /api/books

# Барлық ойындар
GET /api/games
```

---

## ⚠️ Ескертулер

1. **Дубликаттар** - Егер материал қазірдің өзінде бар болса, ол өткізіліп жіберіледі
2. **Админ рөлі** - Мәліметтер қосу үшін админ рөлі қажет
3. **Валидация** - Барлық мәліметтер дұрыс форматта болуы керек

---

## 🛠️ Мәліметтерді өзгерту немесе жою

### Админ панелі арқылы:

1. `/admin` бетіне өтіңіз
2. Өзгерткіңіз келетін материалды табыңыз
3. "Өзгерту" батырмасын басыңыз
4. Өзгерістерді енгізіп, "Сақтау" батырмасын басыңыз

### API арқылы:

```bash
# Материалды өзгерту
PUT /api/movies
{
  "id": "material-id",
  "title": "Жаңа атау",
  ...
}

# Материалды жою
DELETE /api/movies?id=material-id
```

---

## 📊 Статистика

Қазір базада:
- ✅ 17+ фильм
- ✅ 5+ кітап
- ✅ 2+ ойын

---

## ❓ Көмек

Егер мәселелер туындаса:
1. Терминалда қателерді қараңыз
2. Браузер консольін тексеріңіз (F12)
3. База байланысын тексеріңіз (`.env.local` файлында `DATABASE_URL`)












