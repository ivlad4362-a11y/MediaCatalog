# Админ рөліне қалай кіруге болады?

## 📋 Алдымен тіркелу керек

Егер сіз әлі тіркелмеген болсаңыз:

1. Сайтқа өтіңіз: `http://localhost:3000`
2. "Кіру" батырмасын басыңыз
3. "Тіркелу" табын таңдаңыз
4. Email және пароль енгізіп тіркеліңіз

---

## 🔑 Админ рөлін қосу әдістері

### 1️⃣ Скрипт арқылы (Ең оңай)

```powershell
npx tsx scripts/make-admin.ts your-email@example.com
```

**Мысал:**
```powershell
npx tsx scripts/make-admin.ts admin@example.com
```

**Нәтиже:**
```
✅ Пайдаланушы админ рөліне ие болды!
📧 Email: admin@example.com
🔑 Рөл: admin
```

---

### 2️⃣ Базаға тікелей SQL арқылы

1. **Prisma Studio ашу:**
   ```powershell
   npm run db:studio
   ```

2. Браузерде `http://localhost:5555` адресін ашыңыз

3. `User` модельін табыңыз

4. Өзгерту келетін пайдаланушыны табыңыз

5. `role` өрісін `admin` деп өзгертіңіз

6. "Save" батырмасын басыңыз

---

### 3️⃣ API арқылы (тек админ үшін)

Егер сізде бірінші админ бар болса, ол басқа пайдаланушыларды админ қыла алады:

```bash
POST /api/users/make-admin
Content-Type: application/json
Authorization: Bearer <token>

{
  "email": "user@example.com"
}
```

---

## ✅ Админ рөлін тексеру

Админ рөліне ие болғаннан кейін:

1. Сайтқа кіріңіз
2. `/admin` бетіне өтіңіз
3. Егер админ болсаңыз, админ панелі ашылады

---

## 🎯 Админ панелі мүмкіндіктері

Админ рөліне ие болғаннан кейін сіз:

- ✅ Фильмдерді қосу, өзгерту, жою
- ✅ Кітаптарды қосу, өзгерту, жою
- ✅ Ойындарды қосу, өзгерту, жою
- ✅ Пайдаланушыларды басқару
- ✅ Басқа пайдаланушыларды админ қылу
- ✅ Статистиканы көру

---

## ⚠️ Ескертулер

1. **Бірінші админ** - Бірінші админді тек скрипт немесе базаға тікелей қосу арқылы құруға болады
2. **Қауіпсіздік** - Админ рөлі бар пайдаланушылар барлық мәліметтерді басқара алады
3. **Рөлді өзгерту** - Админ рөлін `user` рөліне қайтаруға да болады

---

## 🔧 Скрипт қателері

Егер скрипт қате берсе:

1. **Базаға байланыс қатесі:**
   - `.env.local` файлында `DATABASE_URL` дұрыс екенін тексеріңіз
   - PostgreSQL сервері іске қосылғанын тексеріңіз

2. **Пайдаланушы табылмады:**
   - Алдымен сайтта тіркелу керек
   - Email дұрыс екенін тексеріңіз

3. **Prisma қателері:**
   ```powershell
   npm run db:generate
   ```

---

## 📝 Мысал

```powershell
# 1. Тіркелу (егер әлі тіркелмеген болсаңыз)
# Сайтта тіркеліңіз: http://localhost:3000

# 2. Админ рөлін қосу
npx tsx scripts/make-admin.ts your-email@example.com

# 3. Сайтқа кіріп, /admin бетіне өту
# http://localhost:3000/admin
```

---

## ✅ Нәтиже

Админ рөліне ие болғаннан кейін сіз `/admin` бетіне өтіп, барлық мәліметтерді басқара аласыз!












