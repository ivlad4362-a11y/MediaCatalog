# Сайт ашылмау мәселесін түзету

## ❌ Қате: ENOENT: no such file or directory, open '.next/server/pages/_document.js'

Бұл қате `.next` папкасындағы файлдардың жоқ екенін көрсетеді.

## ✅ Шешім:

### 1. Серверді тоқтату
```powershell
Get-Process -Name node -ErrorAction SilentlyContinue | Stop-Process -Force
```

### 2. .next папкасын жою
```powershell
Remove-Item -Path ".next" -Recurse -Force
```

### 3. Серверді қайта іске қосу
```powershell
npm run dev
```

Сервер іске қосылғанда `.next` папкасы автоматты түрде қайта құрылады.

## ⚠️ Ескертулер:

1. **Build уақыты** - Бірінші рет іске қосылғанда build біраз уақыт алуы мүмкін
2. **Порт** - Сервер `http://localhost:3000` адресінде іске қосылады
3. **Қателер** - Егер қателер болса, терминалда көрсетіледі

## 🔍 Егер мәселелер жалғаса:

1. **Терминалда қателерді қараңыз** - қандай қателер көрсетілгенін тексеріңіз
2. **Браузер консольін тексеріңіз** - F12 басып, Console табын қараңыз
3. **Серверді қайта іске қосыңыз** - Ctrl+C басып, қайта `npm run dev`

## ✅ Нәтиже:

Сервер қайта іске қосылғанда сайт жұмыс істеуі керек!












