# WSL жаңарту нұсқаулығы

## 🔍 Мәселе

Docker Desktop "WSL needs updating" деген хабарлама көрсетіп тұр.

## ✅ Шешім

### 1️⃣ WSL-ді жаңарту

PowerShell-ді **Administrator ретінде** ашып, мына команданы орындаңыз:

```powershell
wsl --update
```

### 2️⃣ Егер команда жұмыс істемесе

**Әдіс 1: Windows Features арқылы**

1. `Win + R` басыңыз
2. `optionalfeatures` енгізіп, Enter басыңыз
3. Мына опцияларды таңдаңыз:
   - ☑️ **Windows Subsystem for Linux**
   - ☑️ **Virtual Machine Platform**
4. **OK** басып, компьютерді қайта қосыңыз

**Әдіс 2: Microsoft Store арқылы**

1. **Microsoft Store-ды ашыңыз**
2. **"WSL" деп іздеңіз**
3. **"Windows Subsystem for Linux" орнатыңыз**
4. **Компьютерді қайта қосыңыз**

### 3️⃣ WSL 2-ні default ретінде орнату

WSL орнатылғаннан кейін:

```powershell
wsl --set-default-version 2
```

### 4️⃣ Docker Desktop-ты қайта іске қосу

1. **Docker Desktop-ты жабыңыз**
2. **Docker Desktop-ты қайта іске қосыңыз**
3. **Енді WSL хабарламасы жоқ болуы керек**

---

## 🔧 Толық тексеру тізбегі

### 1. WSL версиясын тексеру

```powershell
wsl --version
```

Егер версия көрсетілсе, WSL орнатылған! ✅

### 2. WSL 2-ні default ретінде орнату

```powershell
wsl --set-default-version 2
```

### 3. Docker Desktop Settings-ті тексеру

1. **Docker Desktop Settings-ті ашыңыз** (трейде Docker иконкасы → Settings)
2. **General** табын табыңыз
3. **"Use WSL 2 based engine"** опциясын тексеріңіз (таңдалған болуы керек)

### 4. Docker Desktop-ты қайта іске қосу

1. **Docker Desktop-ты жабыңыз**
2. **Docker Desktop-ты қайта іске қосыңыз**
3. **Трейде Docker иконкасы көрінуі керек** 🐳

---

## ⚠️ Ескертулер

1. **PowerShell-ді Administrator ретінде ашу керек** - WSL командалары үшін
2. **Компьютерді қайта қосу қажет** - WSL орнату/жаңарту кезінде
3. **Docker Desktop-ты қайта іске қосу** - WSL жаңарту кезінде

---

## 🎯 Келесі қадамдар

WSL жаңартылғаннан кейін:

1. ✅ **Docker Desktop-ты қайта іске қосыңыз**
2. ✅ **WSL хабарламасы жоқ болуы керек**
3. ✅ **Docker Compose арқылы проектті іске қосыңыз:**
   ```powershell
   docker-compose up -d
   ```

---

## ❓ Көмек

### WSL командалары жұмыс істемейді

- PowerShell-ді **Administrator ретінде** ашыңыз
- Windows Features арқылы WSL-ді қосыңыз

### Docker Desktop әлі де WSL хабарламасын көрсетеді

- Docker Desktop-ты қайта іске қосыңыз
- Компьютерді қайта қосыңыз
- Docker Desktop Settings → General → "Use WSL 2 based engine" тексеріңіз





