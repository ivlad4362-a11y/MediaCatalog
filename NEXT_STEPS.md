# Келесі қадамдар - База деректерін дайындау

## ✅ Орындалған:

1. ✅ `.env.local` файлы құрылды
2. ✅ PostgreSQL паролі қойылды (`Jansaya31-`)
3. ✅ `DATABASE_URL` дұрыс орнатылған

## 📋 Келесі қадамдар:

### 1. JWT_SECRET-ті өзгерту (міндетті емес, бірақ ұсынылады)

`.env.local` файлында `JWT_SECRET`-ті өзгертіңіз:

**Қазір:**
```env
JWT_SECRET="your-secret-key-change-in-production-min-32-chars-long-please-change"
```

**Мысал (кемінде 32 таңба):**
```env
JWT_SECRET="mediacatalog-secret-key-2024-jwt-tokens-secure-random-string"
```

### 2. PostgreSQL базасын құру

Терминалда мына командаларды орындаңыз:

```powershell
# PostgreSQL-ге кіріңіз
psql -U postgres
```

Егер пароль сұраса, `Jansaya31-` енгізіңіз.

Содан кейін PostgreSQL-де:

```sql
-- Базаны құру
CREATE DATABASE mediacatalog;

-- Шығу
\q
```

### 3. Prisma схемасын базаға қосу

Терминалда (VS Code терминалында):

```powershell
# 1. Prisma клиентін генерациялау
npm run db:generate

# 2. Базаға схеманы қосу
npm run db:push
```

### 4. Серверді іске қосу

```powershell
npm run dev
```

Сервер `http://localhost:3000` адресінде іске қосылады.

### 5. Тестілеу

Браузерде:
- `http://localhost:3000` - басты бет
- `http://localhost:3000/api/movies` - API тексеру

## ⚠️ Егер қателер болса:

### "Can't reach database" қатесі:

1. PostgreSQL сервері іске қосылғанын тексеріңіз:
   ```powershell
   # Windows Services-та PostgreSQL сервисін тексеру
   Get-Service -Name "*postgresql*"
   ```

2. База құрылғанын тексеріңіз:
   ```powershell
   psql -U postgres -l
   ```

3. Парольді дұрыс қойғаныңызды тексеріңіз

### "PrismaClientInitializationError" қатесі:

1. `npm run db:generate` орындаңыз
2. `npm run db:push` орындаңыз
3. Серверді қайта іске қосыңыз

## 🎉 Дайын!

Барлығы дұрыс болса, сайт жұмыс істейді!












