# Docker-мен проектті іске қосу

## ✅ Дайындық тексерілді

- ✅ WSL орнатылған (версия 2.6.2.0)
- ✅ Docker жұмыс істеп тұр (версия 29.1.2)
- ✅ Docker Compose жұмыс істеп тұр (версия 2.40.3)

## 🚀 Docker-мен проектті іске қосу

### 1️⃣ Docker Desktop-ты қайта іске қосу

1. **Docker Desktop-ты жабыңыз** (егер ашық болса)
2. **Docker Desktop-ты қайта іске қосыңыз**
3. **Трейде Docker иконкасы көрінуі керек** 🐳
4. **Бірнеше секунд күтіңіз** (Docker жүктелуі керек)

### 2️⃣ .env файлын тексеру

Проект түбінде `.env` файлын тексеріңіз (немесе жасаңыз):

```env
# Database Configuration
POSTGRES_USER=mediacatalog
POSTGRES_PASSWORD=mediacatalog123
POSTGRES_DB=mediacatalog
POSTGRES_PORT=5432

# Application Configuration
APP_PORT=3000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-change-in-production-min-32-chars
JWT_SECRET=your-secret-key-change-in-production-min-32-chars
NEXT_PUBLIC_BASE_URL=http://localhost:3000
```

### 3️⃣ Docker Compose арқылы іске қосу

**Әдіс 1: Автоматты скрипт (ұсынылады)**

```powershell
.\docker-start.ps1
```

**Әдіс 2: Қолмен командалар**

```powershell
# Контейнерлерді іске қосу
docker-compose up -d

# Логтарды көру (не істеп жатқанын бақылау)
docker-compose logs -f
```

### 4️⃣ Сайтты ашу

Браузерде ашыңыз: **http://localhost:3000**

---

## 📋 Пайдалы командалар

### Контейнерлерді басқару

```powershell
# Барлық контейнерлерді көру
docker ps

# Контейнерлерді тоқтату
docker-compose down

# Контейнерлерді қайта іске қосу
docker-compose restart

# Логтарды көру
docker-compose logs -f app
```

### База деректерін басқару

```powershell
# Базаға кіру
docker exec -it mediacatalog-db psql -U mediacatalog -d mediacatalog

# Prisma Studio іске қосу
docker-compose exec app npx prisma studio
# Содан кейін браузерде http://localhost:5555 ашыңыз
```

---

## ⚠️ Ескертулер

1. **Docker Desktop іске қосылған болуы керек** - Трейде Docker иконкасы көрінуі керек
2. **.env файлы дұрыс болуы керек** - База конфигурациясы үшін
3. **Порт 3000 бос болуы керек** - Егер бос емес болса, `.env` файлында `APP_PORT=3001` деп өзгертіңіз

---

## 🎯 Келесі қадамдар

1. ✅ **Docker Desktop-ты қайта іске қосыңыз**
2. ✅ **.env файлын тексеріңіз**
3. ✅ **Docker Compose арқылы проектті іске қосыңыз:**
   ```powershell
   docker-compose up -d
   ```
4. ✅ **Сайтты браузерде ашыңыз:** http://localhost:3000

---

## ❓ Мәселелер

### Docker Desktop іске қосылмайды

- Компьютерді қайта қосыңыз
- Docker Desktop Settings → General → "Use WSL 2 based engine" тексеріңіз

### Порт бос емес

```powershell
# .env файлында басқа портты пайдалану
APP_PORT=3001
```

### Базаға қосылу мәселесі

```powershell
# База контейнерін қайта іске қосу
docker-compose restart postgres

# База логтарын көру
docker-compose logs postgres
```





