# Админ ретінде кіру нұсқаулығы

## ⚠️ Маңызды: Алдымен Docker-ды іске қосыңыз!

Docker жұмыс істемей тұрғандықтан, сайт әлі іске қосылмаған. Алдымен Docker-ды іске қосып, сайтты бастау керек.

## 📋 Қадамдар

### 1️⃣ Docker-ды іске қосу

```powershell
cd C:\Android\MediaCatalog
docker-compose up -d
```

### 2️⃣ Сайтта тіркелу

1. Браузерде ашыңыз: `http://localhost:3000`
2. "Кіру" батырмасын басыңыз
3. "Тіркелу" табын таңдаңыз
4. Мына мәліметтерді енгізіңіз:
   - **Email:** `admin@example.com` (немесе кез келген email)
   - **Пароль:** `admin123` (немесе кез келген пароль)
5. "Тіркелу" батырмасын басыңыз

### 3️⃣ Админ рөлін қосу

PowerShell-де мына команданы орындаңыз:

```powershell
npm run make-admin admin@example.com
```

Немесе:

```powershell
npx tsx scripts/make-admin.ts admin@example.com
```

**Ескерту:** `admin@example.com` орнына сіз тіркелген email-ді енгізіңіз!

### 4️⃣ Сайтқа кіру

1. Браузерде: `http://localhost:3000`
2. "Кіру" батырмасын басыңыз
3. Тіркелген email және парольді енгізіңіз
4. Кіру

### 5️⃣ Админ панеліне өту

Браузерде: `http://localhost:3000/admin`

## 📝 Мысал логин/пароль

Егер сіз мына мәліметтермен тіркелсеңіз:

- **Email:** `admin@example.com`
- **Пароль:** `admin123`

Содан кейін скрипт арқылы админ рөлін қосқаннан кейін:

- **Логин:** `admin@example.com`
- **Пароль:** `admin123`

## ⚡ Жылдам бастау

```powershell
# 1. Docker-ды іске қосу
docker-compose up -d

# 2. Бірнеше секунд күту (сайт іске қосылуы үшін)

# 3. Сайтта тіркелу (браузерде)
# http://localhost:3000 - "Тіркелу" батырмасын басып, email және пароль енгізіңіз

# 4. Админ рөлін қосу (PowerShell-де)
npm run make-admin your-email@example.com

# 5. Сайтқа кіріп, /admin бетіне өту
# http://localhost:3000/admin
```

## 🔍 Базада қандай админдер бар екенін көру

```powershell
npm run db:studio
```

Браузерде `http://localhost:5555` адресін ашып, `User` модельін қараңыз.

## ❓ Мәселелер

### "Пайдаланушы табылмады" қатесі:
- Алдымен сайтта тіркелу керек
- Email дұрыс екенін тексеріңіз

### "Базаға байланыс қатесі":
- Docker-ды іске қосыңыз: `docker-compose up -d`
- База контейнері жұмыс істеп тұрғанын тексеріңіз: `docker ps`

### Сайт ашылмайды:
- Docker-ды іске қосыңыз
- Порт 3000 бос екенін тексеріңіз





