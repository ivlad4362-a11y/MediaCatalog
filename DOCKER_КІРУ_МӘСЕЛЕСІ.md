# Docker арқылы Сайтқа Кіру Мәселесі

## 🔍 Мәселелерді диагностикалау

### 1. Контейнерлердің статусын тексеру

```bash
# Барлық контейнерлердің статусын көру
docker-compose ps

# Егер контейнерлер жұмыс істемесе, іске қосу
docker-compose up -d

# Контейнерлердің логтарын көру
docker-compose logs app
docker-compose logs postgres
```

### 2. Порт мәселелерін тексеру

```bash
# Порт 3000 бос екенін тексеру
netstat -ano | findstr :3000  # Windows
# немесе
lsof -i :3000  # Linux/Mac

# Егер порт бос емес болса, басқа порт пайдалану
# docker-compose.yml файлында APP_PORT өзгерту
```

### 3. Контейнер ішіндегі мәселелерді тексеру

```bash
# App контейнеріне кіру
docker-compose exec app sh

# Ішінде:
ls -la /app/
ls -la /app/server.js
ls -la /app/app/server.js
ps aux | grep node
```

### 4. Build мәселелерін тексеру

```bash
# Контейнерді қайта құрастыру
docker-compose build --no-cache app

# Build логтарын көру
docker-compose build app 2>&1 | tee build.log
```

---

## 🔧 Жедел шешулер

### Мәселе 1: Контейнер іске қосылмайды

```bash
# 1. Контейнерлерді тоқтату
docker-compose down

# 2. Контейнерлерді жою
docker-compose rm -f

# 3. Қайта құрастыру және іске қосу
docker-compose up -d --build
```

### Мәселе 2: Порт 3000 бос емес

```bash
# Басқа порт пайдалану (мысалы, 3001)
# docker-compose.yml файлында:
# ports:
#   - "3001:3000"
```

### Мәселе 3: server.js файлы табылмады

```bash
# Логтарды көру
docker-compose logs app

# Егер server.js табылмаса, build қатесі болуы мүмкін
docker-compose build --no-cache app
```

### Мәселе 4: База деректерімен байланыс қатесі

```bash
# База контейнерінің жұмыс істеп тұрғанын тексеру
docker-compose ps postgres

# Базаға қосылуды тексеру
docker-compose exec postgres psql -U mediacatalog -d mediacatalog -c "SELECT 1;"
```

---

## 📋 Тексеру тізімі

- [ ] Контейнерлер жұмыс істеп тұрған ба? (`docker-compose ps`)
- [ ] Порт 3000 бос ма? (`netstat -ano | findstr :3000`)
- [ ] Логтарда қателер бар ма? (`docker-compose logs app`)
- [ ] server.js файлы бар ма? (`docker-compose exec app ls -la /app/server.js`)
- [ ] База деректерімен байланыс бар ма? (`docker-compose exec app npx prisma db pull`)

---

## 🚀 Қадамдық нұсқау

### 1. Контейнерлерді тоқтату және қайта құру

```bash
docker-compose down
docker-compose build --no-cache app
docker-compose up -d
```

### 2. Логтарды көру

```bash
docker-compose logs -f app
```

### 3. Браузерде ашу

```
http://localhost:3000
```

Егер порт 3000 бос емес болса, басқа порт пайдаланыңыз.

---

## ⚠️ Жиі кездесетін қателер

### 1. "Cannot connect to the Docker daemon"
**Шешу:** Docker Desktop іске қосылғанын тексеру

### 2. "Port is already allocated"
**Шешу:** Портты өзгерту немесе басқа порт пайдалану

### 3. "server.js file not found"
**Шешу:** Build қатесі - контейнерді қайта құрастыру

### 4. "Database connection error"
**Шешу:** База контейнерінің жұмыс істеп тұрғанын тексеру

---

**Дата:** 2024
**Статус:** Диагностикалау нұсқаулығы дайын ✅

