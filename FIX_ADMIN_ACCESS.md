# Админ панеліне кіру мәселесін шешу

## 🔍 Мәселе

Сайтқа кіре аласыз, бірақ админ панеліне (`/admin`) кіре алмайсыз.

## ✅ Шешім қадамдары

### 1️⃣ Админ рөлін қайта тексеру және қосу

PowerShell-де мына командаларды орындаңыз:

```powershell
# 1. Пайдаланушыны админ қылу (егер әлі қосылмаған болса)
npm run make-admin newadmin@example.com

# Ескерту: newadmin@example.com орнына сіздің email-ді енгізіңіз!
```

### 2️⃣ Базада админ рөлін тікелей тексеру

```powershell
# Базада барлық пайдаланушыларды көру
npm run view-db
```

Немесе Prisma Studio арқылы:

```powershell
npm run db:studio
```

Браузерде `http://localhost:5555` адресін ашып:
1. `User` модельін табыңыз
2. Сіздің email-ді табыңыз
3. `role` өрісін `admin` деп тексеріңіз
4. Егер `user` болса, `admin` деп өзгертіңіз

### 3️⃣ Сайттан шығып, қайта кіру

1. **Сайттан шығыңыз** (Logout)
2. **Браузерді жабыңыз** немесе **Private/Incognito режимінде** ашыңыз
3. **Қайта кіріңіз:**
   - Логин: `newadmin@example.com`
   - Пароль: `newpassword123`
4. **Админ панеліне өтіңіз:** `http://localhost:3000/admin`

### 4️⃣ Браузер кэшін тазалау

Егер әлі де жұмыс істемесе:

1. **Ctrl + Shift + Delete** басыңыз
2. **Cookies** және **Cached images** таңдаңыз
3. **Clear data** басыңыз
4. **Сайтқа қайта кіріңіз**

---

## 🔧 Толық тексеру

### Базада админ рөлі бар ма?

```powershell
# PowerShell-де
npm run reset-admin
```

Бұл скрипт барлық админдерді көрсетеді және рөлді өзгертуге мүмкіндік береді.

### Аутентификация токенін тексеру

Браузерде Developer Tools ашып (F12), Console-да:

```javascript
// Cookie-ді тексеру
document.cookie

// Аутентификация API-ны тексеру
fetch('/api/auth/me')
  .then(r => r.json())
  .then(data => console.log('Current user:', data))
```

Егер `role: "admin"` көрсетілмесе, админ рөлі дұрыс қосылмаған.

---

## ⚠️ Жиі кездесетін мәселелер

### 1. Админ рөлі дұрыс қосылмаған

**Шешім:**
```powershell
npm run make-admin your-email@example.com
```

### 2. Браузер ескі токенді сақтап қалған

**Шешім:**
- Сайттан шығып, қайта кіріңіз
- Браузер кэшін тазалаңыз

### 3. Базаға байланыс қатесі

**Шешім:**
```powershell
# Docker-ды іске қосу
docker-compose up -d

# База контейнері жұмыс істеп тұрғанын тексеру
docker ps
```

### 4. Email дұрыс емес

**Шешім:**
- Скриптте дұрыс email енгізілгенін тексеріңіз
- Базада қандай email бар екенін көріңіз: `npm run view-db`

---

## 📋 Толық тексеру тізбегі

```powershell
# 1. Базада пайдаланушыларды көру
npm run view-db

# 2. Админ рөлін қосу (email дұрыс екенін тексеріп)
npm run make-admin your-email@example.com

# 3. Базада админдерді көру
npm run reset-admin

# 4. Сайттан шығып, қайта кіріңіз
# 5. /admin бетіне өтіңіз
```

---

## 🎯 Ең маңыздысы

1. ✅ **Email дұрыс екенін тексеріңіз** - Скриптте дұрыс email енгізілгенін
2. ✅ **Базада админ рөлі бар екенін тексеріңіз** - `npm run view-db` арқылы
3. ✅ **Сайттан шығып, қайта кіріңіз** - Ескі токенді жою үшін
4. ✅ **Браузер кэшін тазалаңыз** - Егер әлі де жұмыс істемесе

---

## ❓ Көмек

Егер барлық қадамдарды орындағаннан кейін де мәселе шешілмесе:

1. **Browser Console-да қателерді тексеріңіз** (F12)
2. **Network tab-та API қауыптарын тексеріңіз**
3. **Базада админ рөлі бар екенін қайта тексеріңіз**





