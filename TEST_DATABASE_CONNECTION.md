# Базаға байланысты тексеру

## 🔍 Мәселе: Authentication failed

База құрылған (pgAdmin-де көрініп тұр), бірақ Prisma байланыса алмайды.

## ✅ Шешімдер:

### 1. Парольді тексеру

pgAdmin-де базаға байланысқанда қандай пароль пайдаланғаныңызды тексеріңіз.

`.env.local` файлында пароль дұрыс екенін тексеріңіз:

```env
DATABASE_URL="postgresql://postgres:Jansaya31-@localhost:5432/mediacatalog?schema=public"
```

**Маңызды:**
- Парольде арнайы таңбалар болса, оларды URL-encode ету керек
- Мысал: `@` → `%40`, `#` → `%23`, `%` → `%25`

### 2. Парольде арнайы таңбалар болса

Егер парольде `@`, `#`, `%`, `&` сияқты таңбалар болса:

**Мысал:**
- Пароль: `Jansaya31-@`
- URL-encode: `Jansaya31-%40`

**Кесте:**
- `@` → `%40`
- `#` → `%23`
- `%` → `%25`
- `&` → `%26`
- `+` → `%2B`
- `=` → `%3D`

### 3. pgAdmin-де парольді тексеру

1. pgAdmin-де PostgreSQL серверіне оң жақты шертіңіз
2. "Properties" таңдаңыз
3. "Connection" табында парольді көріңіз
4. Осы парольді `.env.local` файлына қойыңыз

### 4. База атын тексеру

pgAdmin-де база аты `MediaCatalog` (үлкен әріптермен) болуы мүмкін, ал `.env.local` файлында `mediacatalog` (кіші әріптермен).

**Тексеру:**
- pgAdmin-де база аты: `MediaCatalog` → `.env.local`-да да `MediaCatalog` болуы керек
- Немесе: `mediacatalog` → `.env.local`-да да `mediacatalog` болуы керек

### 5. Пайдаланушы атын тексеру

pgAdmin-де қандай пайдаланушы атымен байланысқаныңызды тексеріңіз.

Әдепкі: `postgres`
Басқа: `Jansaya` немесе басқа аты

## 🔧 Түзету:

### Мысал 1: Парольде арнайы таңбалар жоқ

```env
DATABASE_URL="postgresql://postgres:Jansaya31@localhost:5432/mediacatalog?schema=public"
```

### Мысал 2: Парольде `@` бар

```env
DATABASE_URL="postgresql://postgres:Jansaya31%40@localhost:5432/mediacatalog?schema=public"
```

### Мысал 3: База аты үлкен әріптермен

```env
DATABASE_URL="postgresql://postgres:Jansaya31-@localhost:5432/MediaCatalog?schema=public"
```

## ✅ Тестілеу:

Парольді түзеткеннен кейін:

```powershell
npm run db:push
```

Егер қателер болмаса, бәрі дұрыс!












